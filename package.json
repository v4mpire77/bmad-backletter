{
  "name": "blackletter-automation-tests",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "test": "pnpm -r run test",
    "test:watch": "pnpm -r run test:watch",
    "type-check": "pnpm -r run type-check",
    "clean": "pnpm -r run clean",
    "swa:start": "swa start",
    "swa:build": "swa build",
    "swa:deploy": "swa deploy",
    "bmad:refresh": "bmad refresh",
    "bmad:list": "bmad list",
    "bmad:validate": "test -f AGENTS.md && test -d .bmad-core"
  },
  "dependencies": {
    "bmad-method": "^4.41.0",
    "codex": "^0.2.3",
    "js-yaml": "^4.1.0",
    "micromatch": "^4.0.8"
  },
  "devDependencies": {
    "@octokit/core": "^7.0.3",
    "@octokit/plugin-paginate-rest": "11.3.3",
    "eslint": "9.9.0",
    "prettier": "3.3.3",
    "vitest": "1.6.0"
  }
}
