import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Blackletter | Contract Analysis Demo",
  description: "Explore Blackletter's mock demo: dashboard, findings, and export preview.",
  openGraph: {
    title: "Blackletter Demo",
    description: "Try the mock flow: Dashboard → Findings → Export.",
    type: "website",
    url: "https://blackletter.local/demo",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
