{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "document.extracted.v1",
  "type": "object",
  "required": ["meta", "payload"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["message_id", "analysis_id", "correlation_id", "producer", "ts", "topic", "schema"],
      "properties": {
        "message_id": {"type": "string"},
        "analysis_id": {"type": "string"},
        "correlation_id": {"type": "string"},
        "causation_id": {"type": "string"},
        "producer": {"type": "string"},
        "ts": {"type": "string"},
        "topic": {"const": "document.extracted.v1"},
        "schema": {"type": "string"},
        "replay": {"type": "boolean"}
      }
    },
    "payload": {
      "type": "object",
      "required": ["text", "sentences"],
      "properties": {
        "text": {"type": "string"},
        "sentences": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "text", "start_offset", "end_offset"],
            "properties": {
              "id": {"type": "string"},
              "text": {"type": "string"},
              "start_offset": {"type": "integer"},
              "end_offset": {"type": "integer"}
            }
          }
        },
        "page_map": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["page", "start_offset", "end_offset"],
            "properties": {
              "page": {"type": "integer"},
              "start_offset": {"type": "integer"},
              "end_offset": {"type": "integer"}
            }
          }
        }
      }
    }
  }
}
